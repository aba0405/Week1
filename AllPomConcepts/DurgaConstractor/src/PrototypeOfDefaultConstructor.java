
public class PrototypeOfDefaultConstructor {
	
	/***
	 * if we don't write any constructor the COMPILER 
	 * will create a default constructor itself.
	 * 
	 * Default constructor always no arg constructor
	 * Access modifier for Default constructor same as class
	 * 
	 * Default contains only one line super(). It is a no-arg call
	 * to super class constructor
	 * 
	 * First line for every constructor should be 
	 * this or super. If we don't say anything
	 * compiler will put super.
	 */
	
	

}
